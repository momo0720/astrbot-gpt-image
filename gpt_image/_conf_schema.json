{
  "api_base_url": {
    "description": "Shop API 基础地址，兼容 /models 与 /images/generations 接口",
    "type": "string",
    "default": "https://example.com"
  },
  "api_keys": {
    "description": "多个 Shop API Key。每次请求按 key_strategy 选择其中一个。",
    "type": "list",
    "default": [
      "YOUR_API_KEY"
    ]
  },
  "api_key": {
    "description": "旧版单个 API Key。仅当 api_keys 为空时使用。",
    "type": "string",
    "default": "YOUR_API_KEY"
  },
  "text_model": {
    "description": "文生图模型名称",
    "type": "string",
    "default": "gpt-image-2"
  },
  "size": {
    "description": "文生图尺寸，例如 1024x1024",
    "type": "string",
    "default": "1024x1024"
  },
  "image_quality": {
    "description": "画图质量参数，默认 high；如接口不支持可留空。",
    "type": "string",
    "default": "high"
  },
  "output_format": {
    "description": "输出图片格式，默认使用无损 PNG；可选 jpeg/png/webp。",
    "type": "string",
    "default": "png"
  },
  "stream_image_generation": {
    "description": "是否使用流式画图。打开=stream=true 并按 SSE 读取 completed；关闭=非流式 JSON 响应。",
    "type": "bool",
    "default": true
  },
  "key_strategy": {
    "description": "API Key 选择策略，可选 random 或 round_robin",
    "type": "string",
    "default": "random"
  },
  "models_cache_ttl": {
    "description": "模型列表缓存时间（秒），避免每次都调用 /models 接口",
    "type": "int",
    "default": 300
  },
  "timeout": {
    "description": "请求超时时间（秒）。默认 600 秒；超时后直接失败，不自动重试。",
    "type": "int",
    "default": 600
  }
}
